import React from "react";

const Error = ({ num }) => {
    return (
        <div>
            <h1>ERROR {num}</h1>
            <h6>Oups! It seems like there is a problem!</h6>
        </div>
    );
};

export default Error;
